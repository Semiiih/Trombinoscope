meta {
  name: Import CSV (admin)
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/students/import
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  file: @file()
}
